# ===========================================
# DISCOVER Backend Dependencies
# ===========================================
# Python 3.11+ requis
# Install : pip install -r requirements.txt
# ===========================================

# ============= Framework web =============
flask>=3.0.0
flask-cors>=6.0.0

# ============= LLM =============
# SDK OpenAI (utilisé par le backend LLM 'openai' optionnel)
openai>=1.0.0
# Client HTTP (utilisé pour le harness OpenCode + health checks)
httpx>=0.27.0

# ============= Utilitaires =============
python-dotenv>=1.0.0
pydantic>=2.0.0
